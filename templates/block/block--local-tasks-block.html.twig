{% extends "block.html.twig" %}
{#
/**
 * @file
 * Theme override for tabs.
 */
#}
{% block content %}
  {% if content %}
<nav class="bg-gray-200 shadow dark:bg-gray-800 max-w-full">
    <div class="container min-w-full flex items-center justify-center py-2 mx-auto text-gray-600 capitalize dark:text-gray-300">
      {{ content }}
      </div>
    </nav>
  {% endif %}
{% endblock %}
