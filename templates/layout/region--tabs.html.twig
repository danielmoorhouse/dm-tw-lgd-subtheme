
{% if content %}
<style id="preview"></style>
  <div{{ attributes.addClass(classes) }}>
    <div class="region__inner {{ bg_primary }} region__inner--{{ region|clean_class }}">
    <div class="fixed inline-block z-20 top-20 pt-2 px-4">
      <button onclick="toggleStyleDropdown();"
    class="inline-block p-1 text-gray-700 bg-white hover:bg-gray-50 focus:relative"
    title="Edit theme colour palette"
  >
    <img src="/themes/custom/dm_tw_sub/assets/ui_icons/palette.svg" alt="">
  </button>
  <button
    class="inline-block border-e p-1 text-gray-700 bg-white hover:bg-gray-50 focus:relative"
    title="Edit Product"
  >
    <img src="/themes/custom/dm_tw_sub/assets/ui_icons/text_fields.svg" alt="">
  </button>


    {# <button onclick="toggleStyleDropdown();" class="relative block p-1 text-gray-900 bg-yellow-200 border border-transparent rounded-md dark:text-white focus:border-blue-500 focus:ring-opacity-40 dark:focus:ring-opacity-40 focus:ring-blue-300 dark:focus:ring-blue-400 focus:ring dark:bg-gray-800 focus:outline-none">
        <i class="fa-solid fa-palette"></i>
        <span class="sr-only">Open microsite colour palette menu</span>
    </button> #}
    <!-- Dropdown menu -->
    <div id="dm_tw_ms_style" class="absolute hidden left-0 z-50 p-3 origin-top-left bg-slate-50 shadow-xl w-80">
    {% set groupIDAr = drupal_view('dm_group_id') %}
    {% set groupID = groupIDAr|render|striptags|trim|lower %}
     {% set blockIDAr = drupal_view('contextual_style_application_block_id', 'block_1', groupID) %} 
     {% set blockID = blockIDAr|render|striptags|trim|lower %}
   <div id="style-block-id" data-block-id="{{ blockID }}">{{ blockID }}</div>
      {% set theme_style_form = drupal_entity_form('block_content', blockID, 'edit') %}
     <h2 class="text-xl text-blue-800 rounded-lg mb-2 p-3 inline-block">Microsite Colour Palette &nbsp;<i class="fa-solid fa-palette"></i></h2>
      <div class="hidden font-normal rounded-md text-sm p-1 bg-green-200 text-green-950" id="palette_message_success"></div>
      <div class="hidden font-normal rounded-md text-sm p-1 bg-red-200 text-red-950" id="palette_message_error"></div>
   
     {{ theme_style_form }}
      
    </div>

    </div>
     {# {{ dump(theme_style_form) }}  #}
      {{ content }}
    </div>
  </div>
{% endif %}